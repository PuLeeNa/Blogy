# Choreo Endpoint Configuration
version: 0.1

# Service metadata
endpoints:
  # REST API endpoint
  - name: Blog API
    port: 9090
    type: REST
    networkVisibility: Public
    context: /api
    schemaFilePath: openapi.yaml
